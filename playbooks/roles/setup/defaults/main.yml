---
# defaults file for roles/kickstart
#
package_list:
  - git

git_repos:
  rock:
    branch: devel
    dest: "{{ install_dir }}/tfplenum-rock"
    url: https://bitbucket.di2e.net/scm/thisiscvah/tfplenum.git
  deployer: 
    branch: devel
    dest: "{{ install_dir }}/tfplenum-deployer"
    url: https://bitbucket.di2e.net/scm/thisiscvah/tfplenum-deployer.git